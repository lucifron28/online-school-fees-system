# Goal Findings

This log records inaccurate prior completion claims and the evidence-based correction made during the current goal.

| Finding | Prior claim                                                                     | Current evidence                                                                                                                                                                                                       | Correction                                                                                                                                                                                      |
| ------- | ------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-001   | All phases and the final audit passed.                                          | The current main branch has no `src/db/migrations/` files.                                                                                                                                                             | Reopened the audit and made clean-database migration reproducibility an explicit Phase 1 acceptance gate.                                                                                       |
| F-002   | Login accounts are ready for the walkthrough.                                   | Phase 1 login handlers redirected directly without Better Auth. Phase 2 now uses a shared TanStack Form, Better Auth sessions, fixed role routing, server layout guards, and isolated database/HTTP acceptance checks. | Resolved for authentication/RBAC in Phase 2; domain ownership and financial workflows remain later-phase work.                                                                                  |
| F-003   | Assessments and payments are transactional.                                     | `AssessmentService` returns `ass-simulated-001`; `PaymentService` returns `pay-simulated-001`; neither writes through Drizzle.                                                                                         | Reclassified these as prototype services and reopened the persistence work for Phases 5–7.                                                                                                      |
| F-004   | Portal ownership is enforced.                                                   | `PortalService` accepts caller-supplied linked IDs and `getParentChildren` returns all supplied children.                                                                                                              | Reopened ownership as a PostgreSQL-query requirement in the portal phase.                                                                                                                       |
| F-005   | Mock callback idempotency is durable.                                           | `MockPaymentGateway` stores references in a module-level `Map`.                                                                                                                                                        | Reclassified it as process-local prototype state; Phase 7 must persist checkout/callback idempotency.                                                                                           |
| F-006   | Reports are database-backed.                                                    | `computeDashboardSummary` returns hardcoded metrics.                                                                                                                                                                   | Reopened reporting and reconciliation for Phase 8.                                                                                                                                              |
| F-007   | Reset tooling is safe.                                                          | The former `db:reset` used `DATABASE_URL` directly without target separation or confirmation.                                                                                                                          | Added separate test/demo targets, exact confirmations, production refusal, and integration tests in Phase 0.                                                                                    |
| F-008   | Receipts use compliant wording.                                                 | Several screens and README text used “Official Receipt”.                                                                                                                                                               | Replaced current documentation wording with “Payment Acknowledgment Receipt”; remaining UI wording is tracked for repair.                                                                       |
| F-009   | Foundation CI was ready to validate the branch.                                 | The first PR run stopped at pnpm setup because the workflow declared `11` while `package.json` declared `pnpm@11.17.0`.                                                                                                | Pin the action to `11.17.0` so CI has one authoritative pnpm version, then rerun the required checks.                                                                                           |
| F-010   | The existing schema was sufficient for later persistence work.                  | No committed migration existed, several domain statuses were unrestricted text, financial monetary checks were absent, and posted-history relationships could cascade.                                                 | Added the normalized PostgreSQL contract, committed Drizzle migration, financial constraints, enum columns, and non-destructive financial foreign keys in Phase 1.                              |
| F-011   | Local database commands were equivalent to remote verification.                 | Drizzle Kit initially selected the Neon driver and could not connect to an isolated localhost PostgreSQL database; `db:seed` failed for the same reason.                                                               | Added the pg migration-tool dependency and an explicit localhost adapter path while retaining Neon for remote URLs; migration, verification, seed, and test reset now pass locally.             |
| F-012   | Administration screens represented a finished configuration and user directory. | The settings and users pages stored fixed values in client state and did not persist institution, academic, role, or active-status changes.                                                                            | Phase 3 replaces those prototypes with administrator-only Route Handlers, PostgreSQL-backed services, supported Better Auth account creation, and isolated persistence/constraint verification. |
